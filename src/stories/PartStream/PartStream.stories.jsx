import React from 'react';
import PartStream from '../../components/PartStream';

export default {
  title: 'Part Stream',
  component: PartStream,
};

const Template = () => <PartStream />;

export const Default = Template.bind({});
